# =============================================================================
# NETLIFY ENVIRONMENT VARIABLES FOR MARKETING SITE (apps/web)
# Copy these to your Netlify marketing site's environment variables section
# =============================================================================

# =============================================================================
# BUILD CONFIGURATION
# =============================================================================
NODE_ENV=production
NODE_VERSION=22
NPM_VERSION=10.7.0
NPM_FLAGS=--include=dev

# Build optimization
TURBO_TELEMETRY_DISABLED=1
NETLIFY_CACHE_ENABLED=true
NETLIFY_BUILD_DEBUG=false

# Dependency management
SKIP_PATCHES=false
INSTALL_PLAYWRIGHT=false

# =============================================================================
# PUBLIC VARIABLES (exposed to browser)
# =============================================================================
NEXT_PUBLIC_PROJECT=signtusk
NEXT_PUBLIC_WEBAPP_URL=https://app.yourdomain.com
NEXT_PUBLIC_MARKETING_URL=https://yourdomain.com
NEXT_PUBLIC_DOCS_URL=https://docs.yourdomain.com

# =============================================================================
# ANALYTICS & TRACKING (Optional)
# =============================================================================
# PostHog Analytics - Leave blank to disable
NEXT_PUBLIC_POSTHOG_KEY=your-posthog-project-key
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Google Analytics (Optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# FEATURE FLAGS (Optional)
# =============================================================================
NEXT_PUBLIC_FEATURE_BILLING_ENABLED=true
NEXT_PUBLIC_DISABLE_SIGNUP=false

# =============================================================================
# CONTENT & CMS (Optional)
# =============================================================================
# If using headless CMS for marketing content
CMS_API_URL=https://your-cms-api.com
CMS_API_KEY=your-cms-api-key

# =============================================================================
# CONTACT FORMS & LEAD CAPTURE (Optional)
# =============================================================================
# For contact forms on marketing site
NEXT_PRIVATE_CONTACT_FORM_WEBHOOK=https://hooks.zapier.com/hooks/catch/your-webhook
NEXT_PRIVATE_MAILCHIMP_API_KEY=your-mailchimp-api-key
NEXT_PRIVATE_MAILCHIMP_LIST_ID=your-list-id

# =============================================================================
# SECURITY HEADERS & CSP (Production)
# =============================================================================
NEXT_PUBLIC_CSP_NONCE=auto-generated-by-netlify

# =============================================================================
# DEPLOYMENT CONTEXT VARIABLES
# =============================================================================
# These are automatically set by Netlify but can be overridden
# CONTEXT=production|deploy-preview|branch-deploy
# DEPLOY_PRIME_URL=https://your-deploy-url.netlify.app
# URL=https://your-site.netlify.app

# =============================================================================
# NOTES FOR CONFIGURATION:
# =============================================================================
# 1. Replace all "yourdomain.com" with your actual domain
# 2. Generate secure keys for any API keys and secrets
# 3. Configure analytics keys only if you're using those services
# 4. Test in deploy preview before setting in production
# 5. Use Netlify's environment variable UI for sensitive values