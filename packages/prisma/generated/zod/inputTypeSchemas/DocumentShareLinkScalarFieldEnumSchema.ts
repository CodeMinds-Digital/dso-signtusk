import { z } from 'zod';

export const DocumentShareLinkScalarFieldEnumSchema = z.enum(['id','email','slug','envelopeId','createdAt','updatedAt']);

export default DocumentShareLinkScalarFieldEnumSchema;
