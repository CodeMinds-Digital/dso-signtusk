{
  "buildCommand": "cd ../.. && npm ci --ignore-scripts && npx turbo run build --filter=@signtusk/remix",
  "outputDirectory": "build/client",
  "installCommand": "echo 'Dependencies handled by build command'",
  "framework": null,
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/build/(.*)",
      "destination": "/build/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ],
  "headers": [
    {
      "source": "/build/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ],
  "env": {
    "TURBO_TELEMETRY_DISABLED": "1",
    "INSTALL_PLAYWRIGHT": "false",
    "SKIP_PATCHES": "true"
  }
}