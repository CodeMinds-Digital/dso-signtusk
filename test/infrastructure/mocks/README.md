# Mock Implementation Components

This directory contains mock implementations for PDF signing components:

- `pdf-mock.ts` - PDF document operations mock
- `field-mock.ts` - PDF form field lookup mock  
- `crypto-mock.ts` - PKCS#7 signature validation mock
- `index.ts` - Exports for all mock components